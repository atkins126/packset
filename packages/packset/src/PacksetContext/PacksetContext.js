// PacksetContext.js

import React from 'react';

import packsetContextDefaultValue from './PacksetContextDefaultValue';

const PacksetContext = React.createContext(packsetContextDefaultValue);

export default PacksetContext;
